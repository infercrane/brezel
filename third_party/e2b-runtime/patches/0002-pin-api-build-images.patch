diff --git a/packages/api/Dockerfile b/packages/api/Dockerfile
index 9cac6e3..47d03c9 100644
--- a/packages/api/Dockerfile
+++ b/packages/api/Dockerfile
@@ -1,11 +1,9 @@
-ARG GOLANG_VERSION=1.26.8
-ARG ALPINE_VERSION=3.24
+ARG API_BUILDER_IMAGE=golang:1.26.8-alpine3.24@sha256:6e5de3f5b9fb7e30b8bb2ffe8dcbcbdaa2990f0f31267456eabe83f870a623be
+ARG API_RUNTIME_IMAGE=alpine:3.24@sha256:79ff19e9084a00eece421b2523fb93e22d730e2c0e525905de047e848e56d95f
 
-FROM --platform=$BUILDPLATFORM golang:${GOLANG_VERSION}-alpine${ALPINE_VERSION} AS builder
+FROM ${API_BUILDER_IMAGE} AS builder
 ARG TARGETARCH
 
-RUN apk add --no-cache make
-
 # Copy all module dependency files at once for better Docker layer caching
 WORKDIR /build
 COPY ./shared/go.mod ./shared/go.sum ./shared/
@@ -35,16 +33,13 @@ WORKDIR /build/api
 ARG COMMIT_SHA
 ARG VERSION
 ARG EXPECTED_MIGRATION_TIMESTAMP
-RUN --mount=type=cache,target=/root/.cache/go-build GOARCH=${TARGETARCH} make build COMMIT_SHA=${COMMIT_SHA} VERSION=${VERSION} EXPECTED_MIGRATION_TIMESTAMP=${EXPECTED_MIGRATION_TIMESTAMP}
+RUN --mount=type=cache,target=/root/.cache/go-build \
+    CGO_ENABLED=0 GOARCH=${TARGETARCH} go build -o bin/api \
+    -ldflags "-X=main.commitSHA=${COMMIT_SHA} -X=main.expectedMigrationTimestamp=${EXPECTED_MIGRATION_TIMESTAMP} -X=main.serviceVersion=${VERSION}" .
 
 RUN chmod +x /build/api/bin/api
 
-FROM alpine:${ALPINE_VERSION}
-# Bump this date to force a rebuild from here down, so the upgrade below
-# resolves against the current Alpine package index. Nothing reads the value.
-ENV LAST_FORCED_UPDATE=2026-09-11
-
-RUN apk upgrade --no-cache
+FROM ${API_RUNTIME_IMAGE}
 
 COPY --from=builder /build/api/bin/api .
 
