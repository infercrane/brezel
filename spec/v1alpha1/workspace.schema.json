{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sandbox.runtime.dev/spec/v1alpha1/workspace.schema.json",
  "title": "Create durable workspace",
  "description": "Request body accepted by POST /v1/workspaces. A workspace is project-scoped, single-writer, and outlives any attached sandbox.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name"],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$",
      "maxLength": 128
    }
  }
}
